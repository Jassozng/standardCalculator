const sum = (x, y) => {
    return x + y;
};

const sub = (x, y) => {
    return x - y;
};

const mult = (x, y) => {
    return x * y;
};

const div = (x, y) => {
    return x * y;
};

export default { sum, sub, mult, div };